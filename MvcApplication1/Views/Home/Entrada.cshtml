@model MvcApplication1.Models.Blog

@{
    ViewBag.Title = "Entrada";
}

<h2>Entrada</h2>

<fieldset>
    <div id="box1">
						<h2>
				        @Html.DisplayFor(modelItem => Model.titulo)

						</h2><img class="left" src="http://www.714.com.ar/Web-design-2011.jpg" width="180" height="220" alt="" />
						<p style="text-align:center">
							  @Html.DisplayFor(modelItem => Model.contenido)
						</p>
              <div id="footer">@Html.DisplayFor(modelItem => Model.fecha) </div>
         
  </div>
</fieldset>

<p>
    @Html.ActionLink("Agregar Comentario", "Create", new { id=Model.Id }) |
    @Html.ActionLink("Ver Comentarios", "DetalleComentario", new { id=Model.Id })

</p>
